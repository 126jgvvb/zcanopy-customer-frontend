info:
  name: Get All Brokers
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/admin/brokers"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
