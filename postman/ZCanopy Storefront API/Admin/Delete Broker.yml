info:
  name: Delete Broker
  type: http
  seq: 18

http:
  method: DELETE
  url: "{{baseUrl}}/admin/brokers/{{brokerId}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
