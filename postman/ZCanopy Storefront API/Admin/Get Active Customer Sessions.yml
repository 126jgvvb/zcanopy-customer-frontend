info:
  name: Get Active Customer Sessions
  type: http
  seq: 33

http:
  method: GET
  url: "{{baseUrl}}/admin/customers/active-sessions"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
