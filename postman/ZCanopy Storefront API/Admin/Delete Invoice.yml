info:
  name: Delete Invoice
  type: http
  seq: 35

http:
  method: DELETE
  url: "{{baseUrl}}/admin/invoices/{{invoiceId}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
