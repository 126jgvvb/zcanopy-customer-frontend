info:
  name: Get Property Locations
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/admin/property/locations"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
