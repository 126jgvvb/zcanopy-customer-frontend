info:
  name: Approve All Pending Verifications
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}/admin/pending/verifications/approve-all"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
