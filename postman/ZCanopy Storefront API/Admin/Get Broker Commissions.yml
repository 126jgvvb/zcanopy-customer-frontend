info:
  name: Get Broker Commissions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/admin/broker-commissions"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
