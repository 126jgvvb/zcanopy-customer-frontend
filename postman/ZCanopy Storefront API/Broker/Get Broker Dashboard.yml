info:
  name: Get Broker Dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/web/broker/dashboard"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
