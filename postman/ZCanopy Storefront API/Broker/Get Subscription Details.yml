info:
  name: Get Subscription Details
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/web/broker/subscription"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
