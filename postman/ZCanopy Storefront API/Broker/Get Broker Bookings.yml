info:
  name: Get Broker Bookings
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/web/broker/bookings"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
