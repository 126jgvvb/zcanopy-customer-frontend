info:
  name: Get Broker Notifications
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/web/broker/notifications"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
