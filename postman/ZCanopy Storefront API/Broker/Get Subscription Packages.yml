info:
  name: Get Subscription Packages
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/web/broker/subscription/packages"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
