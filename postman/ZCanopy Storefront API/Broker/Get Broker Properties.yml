info:
  name: Get Broker Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/web/broker/properties"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
