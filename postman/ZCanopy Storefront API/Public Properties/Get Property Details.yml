info:
  name: Get Property Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/web/public/properties/{{propertyId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
