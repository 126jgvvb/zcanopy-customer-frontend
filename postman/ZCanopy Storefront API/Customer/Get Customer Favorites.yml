info:
  name: Get Customer Favorites
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/web/customer/favorites"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
