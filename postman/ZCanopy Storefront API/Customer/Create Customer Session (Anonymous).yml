info:
  name: Create Customer Session (Anonymous)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/customer/session"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
