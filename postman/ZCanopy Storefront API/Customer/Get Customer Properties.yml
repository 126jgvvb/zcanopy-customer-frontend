info:
  name: Get Customer Properties
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/web/customer/properties"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
