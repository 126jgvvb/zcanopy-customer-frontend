info:
  name: Broker Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/web/auth/broker/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "brokerCode": "BRK-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
